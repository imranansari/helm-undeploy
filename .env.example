# Temporal Configuration
TEMPORAL_HOST=localhost:7233
TEMPORAL_NAMESPACE=default
TASK_QUEUE=helm-undeploy-queue

# Kubernetes Configuration
# Target namespace for deployments (required)
KUBERNETES_NAMESPACE=default

# Path to kubeconfig file (optional, defaults to ~/.kube/config)
KUBECONFIG=/path/to/kubeconfig

# K8s Secret Paths (when running in Kubernetes)
# These paths are typically mounted from Kubernetes secrets
KUBECONFIG_SECRET_PATH=/var/secrets/kubeconfig/config

# Helm Configuration
HELM_DRIVER=secrets